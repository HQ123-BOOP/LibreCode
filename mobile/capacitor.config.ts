import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.github.hq123boop.librecode",
  appName: "LibreCode",
  webDir: "www",
  backgroundColor: "#0d0f12",
  android: {
    backgroundColor: "#0d0f12",
  },
};

export default config;
